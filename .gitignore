# Binaries
/aide
/clawpty

# Database files
*.db

# Config with secrets (use config.example.yaml as template)
config.yaml

# Session workspaces
*.db-shm
*.db-wal

# Docker — generated and local overrides (never commit)
compose.override.yml
compose.generated.yml

# OS / editor
.DS_Store
*.swp
*.swo
